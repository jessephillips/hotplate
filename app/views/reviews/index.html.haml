%p#notice= notice
%h1
  Reviews for #{@title}
%table
  %thead
    %tr
      %th Description
      %th Rating
      %th{:colspan => "3"}
  %tbody
    - @reviews.each do |review|
      %tr
        %td= review.description
        %td= review.rating
        %td= link_to 'Show', [@meal, review]
        %td= link_to 'Edit', edit_meal_review_path(@meal, review)
        %td= link_to 'Destroy', [@meal, review], method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Review', new_meal_review_path
