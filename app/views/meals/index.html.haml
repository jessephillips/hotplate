%p#notice= notice
%h1 Plates
%table
  %thead
    %tr
      %th Restaurant
      %th Name
      %th Cost
      %th Servings
      %th Cost per person
      %th{:colspan => "3"}
  %tbody
    - @meals.each do |meal|
      %tr
        %td
          - if meal.restaurant.present?
            = link_to meal.restaurant.name, meal.restaurant
          - else
            %i none
        %td= meal.name
        %td= number_to_currency(meal.cost_in_dollars)
        %td= meal.servings
        %td= number_to_currency(meal.cost_per_serving)
        %td= link_to 'Show', meal
        %td= link_to 'Delete', meal_path(meal), method: :delete, data: {confirm: 'Are you sure?'}
%br/
= link_to 'Add a Plate', new_meal_path
